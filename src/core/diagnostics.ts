// Single switch for anomaly-only diagnostic logs (placeholder-skipped,
// inject-error, unknown-command). Real errors (console.warn) are not
// gated and stay on regardless.
export const VSH_VISHRUN_DIAG = false;
